{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MusicKit desktop default capability — picker page needs the store plugin to remember server URLs, the window-drag command for our custom in-app title bar, and core defaults for everything else.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "store:default"
  ]
}
