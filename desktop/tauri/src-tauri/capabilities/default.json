{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MusicKit desktop default capability — picker page needs the store plugin to remember server URLs, plus window-state for persistence.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "window-state:default"
  ]
}
